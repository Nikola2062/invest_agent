# Runtime dependencies for the Capital Rotation Monitoring System.
# This file is the source of truth; `pyproject.toml` reads from it via
# `[tool.setuptools.dynamic]`.
#
# Install: pip install -r requirements.txt
# Editable install (also creates the `rotate` CLI): pip install -e .

# Core data + storage
yfinance>=1.4.1                  # bars + ETF info; 0.2.x is broken vs current Yahoo API
duckdb>=1.0.0                    # columnar store
pandas>=2.2.2                    # all metric / signal math
pandas-market-calendars>=4.4.0   # NYSE trading-day alignment

# Config + CLI
click>=8.1.7                     # rotate CLI
pyyaml>=6.0.2                    # config.yaml loader

# Report rendering
markdown>=3.5                    # markdown -> HTML in pdf_report.py
fpdf2>=2.8                       # HTML -> PDF (pure Python; no system libs)
openpyxl>=3.1                    # SSGA holdings XLSX (flows_issuer_direct.py)
