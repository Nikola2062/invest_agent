# Copy to .env and fill in. .env is gitignored.
# Telegram bot credentials — used for BOTH the daily push (Mode A) and the
# two-way on-demand bot (Mode B). Get them from @BotFather.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
